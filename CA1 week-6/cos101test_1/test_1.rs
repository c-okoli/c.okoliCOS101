use std::io

fn main() {
	let prices = [
	    ("AL", 1200000),
	    ("ARR", 550000),
	    ("C", 1500000),
	    ("D", 800000),
	    ("ART", 450000),
	    ];

	    println!("Health Diagnosis:");
	    println!("AL = Alzheimer - N1,200,000");
	    println!("ARR = Arrhythmia - N550,000");
	    println!("C = Chronic Kidney Disease - N1,500,000");
	    println!("D = Diabetes - N800,000");
	    println!("ART = Arthritis - N450,000");

	    let mut total_discount = 60.0;

    loop {
        let mut name = String::new();
        println!("Enter name:");
        io::stdin().read_line(&mut name).expect("Failed to read line");
        }

        let mut date of birth = String::new();
        println!("Enter date of birth:");
        io::stdin().read_line(&mut date of birth).expect("Failed to read line");
        }

        let mut email address = String::new();
        println!("Enter email address:");
        io::stdin().read_line(&mut email address).expect("Failed to read line");
        }

        let mut quantity = String::new();
        println!("Enter phone number:");
        io::stdin().read_line(&mut phone number).expect("Failed to read line");
        let quantity:i32 = match quantity.trim().parse() {
            {
            println!("Invalid input. Please enter a number.");
            continue;
        }

        let mut quantity = String::new();
        println!("Enter number of siblings:");
        io::stdin().read_line(&mut number of siblings).expect("Failed to read line");
        let quantity:i32 = match quantity.trim().parse() {
            {
            println!("Invalid input. Please enter a number.");
            continue;
        }

        let mut quantity = String::new();
        println!("Enter number of children:");
        io::stdin().read_line(&mut number of children).expect("Failed to read line");
        let quantity:i32 = match quantity.trim().parse() {
            {
            println!("Invalid input. Please enter a number.");
            continue;
        }

        let mut medical diagnosis = String::new();
        println!("Enter medical diagnosis:");
        io::stdin().read_line(&mut medical diagnosis).expect("Failed to read line");
        }

        let mut village of residence = String::new();
        println!("Enter village of residence:");
        io::stdin().read_line(&mut village of residence).expect("Failed to read line");
        }

        let mut age = String::new();
        println!("Enter age:");
        io::stdin().read_line(&mut age).expect("Failed to read line");
        let age:i32 = age.trim().parse().expect("Invalid input. Please enter a number.");

        //Determine the amount based on the patient's information
        let amount = if patient_alzheimer > 0 {
            if age >= 50.0 {
               children >= 4.0
               village of residence = Akpabom Village
               discount = 20.0
           } else if age = 30.0 {
             siblings >= 4.0
             village of residence = Ngbauji
             discount = 5.0
           } else if age >= 40.0 {
             children >= 3.0

             siblings >= 3.0
             village of residence = Atabrikang
             discount = 15.0
           } else if age >= 28.0
             age <= 45.0
             diagnosis = diabetes
             children = 2:4  
           }
        }
}        